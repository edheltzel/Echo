{
  "name": "echo",
  "description": "Mute Echo audio for every session on this machine via cli/echo mute. Does not install the daemon or register Claude Code lifecycle hooks.",
  "version": "0.1.0"
}
