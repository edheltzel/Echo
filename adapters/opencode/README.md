# Echo adapter for OpenCode

Mute-only: registers `/echo-mute` as `~/.config/opencode/commands/echo-mute.md` → `cli/echo mute`.

```bash
bash scripts/install.sh --adapter opencode
```

Override the commands directory with `ECHO_OPENCODE_COMMANDS_DIR` in tests.
